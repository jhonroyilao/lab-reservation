{
  "name": "lab-room-workspace",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=client",
    "build": "npm run build --workspace=client",
    "preview": "npm run preview --workspace=client",
    "lint": "npm run lint --workspace=client"
  },
  "devDependencies": {
    "eslint": "^9.25.0",
    "prettier": "^3.0.0"
  }
}
